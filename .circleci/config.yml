version: 2.1

orbs:
  maven: circleci/maven@1.0.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - maven/with_cache:
          steps:
            - run: mvn -B clean package

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
